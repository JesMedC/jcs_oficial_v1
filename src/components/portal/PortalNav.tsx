/*
 * ARCHIVED por portal-fase0a-base — see openspec/changes/portal-fase0a-base/proposal.md
 * for the rationale (Desktop vertical sidebar restored; this horizontal
 * mini-nav became redundant once TopNav + the restored PortalSidebar
 * carried the brand + nav + user surface). History preserved so a
 * future rollback can re-light this file with git checkout.
 */
export function PortalNav(): null {
  return null;
}
